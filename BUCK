# Root package targets — minimal; only exposes files needed by toolchain rules
# in other cells (e.g., toolchains//:rust needs clippy.toml).

export_file(
    name = "clippy-toml",
    src = "clippy.toml",
    visibility = ["PUBLIC"],
)
