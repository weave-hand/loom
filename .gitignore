/buck-out
/target
cspell.json
**/target/**
/.vscode/
rust-project.json
rust-project.json.tmp
/.loom/
/.direnv/
# `homelab` git external cell expansion target (buck2 fetches into buck-out;
# `buck2 expand-external-cell homelab` writes here). Never checked in.
/homelab/
**/.cargo/**
!**/.cargo/config.toml
# buck2 per-machine config override (local file watcher, etc.); never checked in
.buckconfig.local
# LLVM coverage profiling data emitted by instrumented binaries at runtime
*.profraw
*.profdata
.claude/worktrees/

# Superpowers SDD scratch (progress ledger, subagent reports)
.superpowers/

# buck2 user-local config override (e.g. file_watcher workaround)
.buckconfig.local

# Planning artifacts live in GitHub Issues since #466; specs/plans must never
# be committed here again (upstream skill defaults still point at this path)
docs/superpowers/
